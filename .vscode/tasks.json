{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ray_tracer",
            "type": "shell",
            "command": "cd cmake && cmake .. && make && ../bin/ray_tracer",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
